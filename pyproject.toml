[project]
name = "scanner-and-qa"
version = "0.1.0"
description = "热键拍照 -> 询问视觉 LLM -> 置顶浮窗流式显示"
requires-python = ">=3.11"
dependencies = [
    "pynput>=1.7.6",
    "openai>=1.30.0",
    "pillow>=10.0.0",
    "opencv-python>=4.8.0",
]

[project.optional-dependencies]
web = ["streamlit>=1.37"]

[tool.uv]
package = false
